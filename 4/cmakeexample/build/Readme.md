# Binaries

## Linux users

`cd ./build`
`cmake ..`
`make`
`./myApplication`

## Mac users

`cd ./build`
`cmake ..`
`make`
`cmake ..` 
`make`  I need to debug, but if you run this twice you should get 'myApplication' created
`./myApplication`
